<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
    <a class="navbar-brand" href="/">
    <img src="<%= asset_path( 'idea.png' ) %>" alt="" width="30" height="30" class="d-inline-block align-top">
    The Idea App
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="nav justify-content-end" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
        <li class="nav-item inactive <%= 'active' if params[:selected] != 'ideas' %>">
            <a class="nav-link" href="/ideas?selected=ideas">Ideas</a>
        </li>
        <li class="nav-item inactive <%= 'active' if params[:selected] != 'info' %>">
            <a class="nav-link" href="/pages/info?selected=info">Info</a>
        </li>
        </ul>
    </div>
    </div>
</nav>
